"""
shared — reusable utilities for weight outlier analysis.

Modules:
    stats   — per-tensor statistical computations
    io      — model loading, FP8 dequantization, data I/O
    viz     — matplotlib dark-theme helpers
    report  — summary computation and markdown generation
"""
